# site/

Static website. Vanilla HTML/CSS/JS, no framework. Hosted on GitHub Pages from this same repo.

Lands in Phase 2. Two views per app (human-readable + raw JSON), two copy-to-clipboard boxes for the seed skills (use + explore).
