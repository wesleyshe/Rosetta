# worker/

Cloudflare Worker (or Vercel Edge Function — TBD) that turns skill submissions into GitHub PRs. Takes a skill spec + GitHub OAuth token, creates a branch, commits, and opens a PR against this repo.

Lands in Phase 6.
